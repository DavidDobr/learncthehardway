#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
    int distance = 100;
    char *test = malloc(100);

    // this is also a comment
    printf("you are %d miles away %s.\n", distance, test);

    return 0;

}

